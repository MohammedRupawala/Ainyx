version: "2"

sql:
  - engine: "postgresql"
    schema: "../migrations"
    queries: "./queries"
    gen:
      go:
        package: "sqlc"
        out: "."
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true